<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dj.mapper.QuestionExtMapper">
  <resultMap id="BaseResultMap" type="com.dj.model.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Sat Mar 28 11:18:50 CST 2020.
    -->
    <id column="ID" jdbcType="INTEGER" property="id" />
    <result column="TITLE" jdbcType="VARCHAR" property="title" />
    <result column="GMT_CREATE" jdbcType="BIGINT" property="gmtCreate" />
    <result column="GMT_MODIFIED" jdbcType="BIGINT" property="gmtModified" />
    <result column="CREATOR" jdbcType="INTEGER" property="creator" />
    <result column="COMMENT_COUNT" jdbcType="INTEGER" property="commentCount" />
    <result column="VIEW_COUNT" jdbcType="INTEGER" property="viewCount" />
    <result column="LIKE_COUNT" jdbcType="INTEGER" property="likeCount" />
    <result column="TAG" jdbcType="VARCHAR" property="tag" />
  </resultMap>
  <update id="incView" parameterType="com.dj.model.Question">
    update QUESTION
    set
    VIEW_COUNT =VIEW_COUNT + #{record.viewCount,jdbcType=INTEGER}
    where id=#{record.id}
  </update>

  <update id="intCommentCount" parameterType="com.dj.model.Question">
    update QUESTION
    set
    COMMENT_COUNT =COMMENT_COUNT + #{record.commentCount,jdbcType=INTEGER}
    where id=#{record.id}
  </update>

  <select id="selectRelated" parameterType="com.dj.model.Question" resultMap="BaseResultMap">
    select * from QUESTION where id != #{record.id} and tag regexp #{record.tag}
  </select>

  <select id="countBySearch" parameterType="com.dj.dto.QuestionQueryDTO" resultType="java.lang.Integer">
    select count(*) from QUESTION
    <where>
      <if test="record.search != null">
        and title regexp #{record.search}
      </if>
    </where>
  </select>

  <select id="selectBySearch" parameterType="com.dj.dto.QuestionQueryDTO" resultMap="BaseResultMap">
    select * from QUESTION
    <where>
      <if test="record.search != null">
        and title regexp #{record.search}
      </if>
    </where>
    order by gmt_create desc limit #{record.page},#{record.size}
  </select>
</mapper>